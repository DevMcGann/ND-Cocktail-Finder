import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  cancel_button: {
    color: 'red',
  },

  text: {
    fontSize: 15,
    fontWeight: 'bold',
  },
});

export default styles;
