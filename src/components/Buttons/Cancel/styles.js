import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  cancelButton: {
    color: 'red',
  },

  text: {
    fontSize: 15,
    fontWeight: 'bold',
  },
});

export default styles;
